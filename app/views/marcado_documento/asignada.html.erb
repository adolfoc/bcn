<%= render "shared/work_header" %>

|&nbsp;
	<%= link_to "Requiere marcaje automatico", requiere_marcaje_automatico_event_path(@task.id) %>&nbsp;|
<% if @ot.am_results.count > 0 %>
	<%= link_to "No requiere marcaje automatico", no_requiere_marcaje_automatico_event_path(@task.id) %>&nbsp;|
<% end %>
