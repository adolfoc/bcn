<%= form_for(@participation_type) do |f| %>
  <% if @participation_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@participation_type.errors.count, "error") %> prohibited this participation_type from being saved:</h2>

      <ul>
      <% @participation_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :uri %><br />
    <%= f.text_field :uri %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
