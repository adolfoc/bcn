<%= form_for(@debate_type) do |f| %>
  <% if @debate_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@debate_type.errors.count, "error") %> prohibited this debate_type from being saved:</h2>

      <ul>
      <% @debate_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ordinal %><br />
    <%= f.number_field :ordinal %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
