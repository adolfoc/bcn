<%= form_for(@taxonomy_category) do |f| %>
  <% if @taxonomy_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@taxonomy_category.errors.count, "error") %> prohibited this taxonomy_category from being saved:</h2>

      <ul>
      <% @taxonomy_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
