<%= form_for(@frbr_bcn_type) do |f| %>
  <% if @frbr_bcn_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@frbr_bcn_type.errors.count, "error") %> prohibited this frbr_bcn_type from being saved:</h2>

      <ul>
      <% @frbr_bcn_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
