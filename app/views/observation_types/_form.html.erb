<%= form_for(@observation_type) do |f| %>
  <% if @observation_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@observation_type.errors.count, "error") %> prohibited this observation_type from being saved:</h2>

      <ul>
      <% @observation_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :ordinal %><br />
    <%= f.number_field :ordinal %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
