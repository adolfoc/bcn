<%= form_for(@frbr_entity) do |f| %>
  <% if @frbr_entity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@frbr_entity.errors.count, "error") %> prohibited this frbr_entity from being saved:</h2>

      <ul>
      <% @frbr_entity.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
