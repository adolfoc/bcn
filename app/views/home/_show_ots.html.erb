<h1><%= table_title %></h1>

<table>
	<thead>
		<tr>
			<th>Número de serie</th>
			<th>OT</th>
			<th>Estado</th>
			<th>Prioridad</th>
			<th>Creada el</th>
			<th>Completada el</th>
		</tr>
	</thead>
	<tbody>
	<% ots.each do |ot| %>
		<tr>
			<td><%= ot.serial_number %></td>
			<td><%= link_to ot.name, mostrar_ot_path(ot.id) %></td>
			<td><%= ot.ot_state.name %></td>
			<td><%= ot.priority.name %></td>
			<td><%= ot.created_on %></td>
			<td><%= ot.completed_on %></td>
		</tr>
	<% end %>
	</tbody>
</table>
